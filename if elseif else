#include <iostream>
using namespace std;
int main() {
    int num;
    cin>>num;
    if (num>=1000)
    {
    cout<<"clg frnd";
    }
    else if (num>=500)
    {
        cout<<"schl frnd";
    }
    else {
    cout<<"home";
}
    return 0;
}
